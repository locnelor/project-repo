{
  "name": "@web/admin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 9090",
    "build": "vite build",
    "preview": "vite preview",
    "clear-cache": "rm -rf node_modules/.vite && rm -rf dist",
    "dev:fresh": "npm run clear-cache && vite"
  },
  "imports": {
    "#/*": "./src/*"
  },
  "dependencies": {
    "@internal/tailwind-config": "workspace:*",
    "@repo/hooks": "workspace:*",
    "@repo/request": "workspace:*",
    "@repo/stores": "workspace:*",
    "@repo/styles": "workspace:*",
    "@repo/utils": "workspace:*"
  }
}
